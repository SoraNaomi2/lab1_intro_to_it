#include <iostream>
using namespace std;

ostream& doneline(ostream& stream) {
    stream << '\n';
    return stream;
}

int main() {
    setlocale(LC_ALL, "ru");
    cout << "РџРµСЂРІР°СЏ СЃС‚СЂРѕРєР°" << doneline << "Р’С‚РѕСЂР°СЏ СЃС‚СЂРѕРєР°";
    return 0;
}